<nav class="navbar navbar-expand-lg navbar-light bg-light">
      <div class="container-fluid">
        <a class="navbar-brand" href="/">NotesApp</a>
        <button
          class="navbar-toggler"
          type="button"
          data-bs-toggle="collapse"
          data-bs-target="#navbarNav"
          aria-controls="navbarNav"
          aria-expanded="false"
          aria-label="Toggle navigation"
        >
          <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarNav">
          <ul class="navbar-nav">
            {{#if user}}
            {{else}}
            <li class="nav-item">
              <a class="nav-link" aria-current="page" href="/users/signin"
                >Log in</a
              >
            </li>
            <li class="nav-item">
              <a class="nav-link" href="/users/signup">Register</a>
            </li>
            {{/if}}
            <li class="nav-item">
              <a class="nav-link" href="/notes">All notes</a>
            </li>
            <li class="nav-item">
              <a class="nav-link" href="/notes/add">crear nota</a>
            </li>
            {{#if user}}
            <li class="nav-item">
              <a class="nav-link" href="/users/logout">Logout</a>
            </li>
            {{/if}}
            {{#if user}}
            <li class="nav-item">
              <span class="nav-link"><strong>- hello {{user.name}}</strong></span>
            </li>
            {{/if}}
          </ul>
        </div>
      </div>
    </nav>
